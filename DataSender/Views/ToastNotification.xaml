<Window x:Class="DataSender.Views.ToastNotification"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="300" Height="100" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize" 
        Loaded="Window_Loaded" Closing="Window_Closing">
    <Border Background="#DD333333" CornerRadius="10" Padding="10">
        <StackPanel>
            <TextBlock x:Name="TitleText" FontWeight="Bold" Foreground="White" FontSize="14"/>
            <TextBlock x:Name="MessageText" Foreground="White" Margin="0,5,0,0"/>
            <ProgressBar x:Name="LoadingBar" Height="10" IsIndeterminate="True" Visibility="Collapsed" Margin="0,10,0,0"/>
        </StackPanel>
    </Border>
</Window>
